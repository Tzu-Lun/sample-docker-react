sudo: required 
language: node_js
node_js:
    - "17"
    
services: 
    - docker
before_install: 
    - docker build -t declanlin/sample-app -f Dockerfile.dev .
script:     
    - docker run -e CI=true declanlin/sample-app npm run test -- --coverage --watchAll=false

deploy:
  provider: elasticbeanstalk
  region: "us-east-1"
  app: "sample-docker-react"
  env: "Travispractice-env" ### Your EB App environment name
  bucket_name: "elasticbeanstalk-us-east-1-218584516923"
  bucket_path: "EBApptest"
  on:
    branch: main
  access_key_id: "$AWS_ACCESS_KEY"
  secret_access_key: "$AWS_SECRET_KEY"